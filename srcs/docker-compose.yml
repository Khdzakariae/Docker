version: '3.8'
services:
  nginx-ssh:
    build: ./requirements/nginx/.
    container_name: nginx-container
    ports:
      - "8080:80"
    volumes:
      - ./requirements/nginx/html/index.html:/usr/share/nginx/html/index.html   # Mount local html directory to container Mount Nginx configuration file
    restart: unless-stopped

